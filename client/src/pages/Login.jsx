import { useState } from "react"; import { ArrowRight, LockKeyhole } from "lucide-react"; import { Link,useNavigate } from "react-router-dom"; import { login } from "../services/api";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const navigate=useNavigate();const submit=async e=>{e.preventDefault();try{const r=await login({email,password});localStorage.setItem("zv_token",r.data.token);localStorage.setItem("zv_user",JSON.stringify(r.data.user));navigate(r.data.user.role==="admin"?"/admin":r.data.user.role==="vendor"?"/vendor/dashboard":"/")}catch(err){alert(err.response?.data?.message||"Invalid credentials.")}};return <section className="center-page"><form className="auth-simple" onSubmit={submit}><div className="brand-mark"><LockKeyhole size={19}/></div><div className="eyebrow">ACCOUNT ACCESS</div><h1>Welcome back.</h1><p className="muted">Sign in securely with your Zvertex3D email.</p><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button primary full">Sign in <ArrowRight size={17}/></button><small className="muted">New here? <Link to="/signup">Create an account.</Link> · Vendor? <Link to="/vendor/register">Apply here.</Link></small></form></section>}
