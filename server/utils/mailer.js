<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
<<<<<<< HEAD
const nodemailer=require("nodemailer"); let transporter=null; function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;} async function sendMail({to,subject,html,text}){const from=process.env.MAIL_FROM||"Zvertex3D <no-reply@zvertex3d.com>";const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from,to,subject,html,text});return {sent:true};} function verifyUrl(token){const base=(process.env.CLIENT_URL||"http://localhost:5173").split(",")[0].trim();return `${base}/verify-email?token=${encodeURIComponent(token)}`;} async function sendVerificationEmail(user,token){const url=verifyUrl(token);return sendMail({to:user.email,subject:"Verify your Zvertex3D email",text:`Welcome to Zvertex3D. Verify your email: ${url}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2 style="color:#071321">Zvertex3D</h2><p>Hi ${user.name||"there"},</p><p>Welcome to Zvertex3D. Please verify your email to activate your account.</p><p><a href="${url}" style="display:inline-block;background:#071321;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Verify email</a></p><p style="color:#64748b">This link expires in 24 hours.</p></div>`});} async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL;if(!to)return {sent:false,reason:"admin_email_missing"};return sendMail({to,subject,html,text});} async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;const text=`Your Zvertex3D vendor application is now ${status}.`;return sendMail({to:vendor.email,subject,text,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p><p>Thank you,<br/>Zvertex3D Admin</p></div>`});} async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject, text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);} module.exports={sendMail,sendVerificationEmail,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
=======
const nodemailer=require("nodemailer");
const COMMUNICATION_EMAIL=process.env.MAIL_FROM||"zvertex3d@gmail.com";
let transporter=null;
function getTransporter(){if(transporter)return transporter;if(!process.env.SMTP_HOST)return null;transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:String(process.env.SMTP_SECURE)==="true",auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});return transporter;}
async function sendMail({to,subject,html,text}){const tx=getTransporter();if(!tx){console.warn(`[Zvertex3D mail] SMTP not configured. To: ${to} | Subject: ${subject}`);return {sent:false,reason:"smtp_not_configured"};}await tx.sendMail({from:COMMUNICATION_EMAIL,to,subject,html,text,replyTo:COMMUNICATION_EMAIL});return {sent:true};}
async function sendSignupConfirmation(user){return sendMail({to:user.email,subject:"Welcome to Zvertex3D",text:`Hi ${user.name||'there'}, your Zvertex3D account has been created successfully. You can sign in now.`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2>Zvertex3D</h2><p>Hi ${user.name||'there'},</p><p>Your Zvertex3D account has been created successfully.</p><p>No email verification is required. You can sign in immediately.</p><p>Thank you,<br>Zvertex3D Team</p></div>`});}
async function sendVendorApplicationConfirmation(vendor){return sendMail({to:vendor.email,subject:"Zvertex3D vendor application received",text:`Hi ${vendor.name||'there'}, your vendor application for ${vendor.storeName} was received and is pending admin approval.`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h2>Zvertex3D Vendor Network</h2><p>Hi ${vendor.name||'there'},</p><p>We received your vendor application for <strong>${vendor.storeName}</strong>.</p><p>Your application is now <strong>pending admin approval</strong>. Once approved, your store will automatically appear in the Zvertex3D Marketplace.</p><p>No email verification is required.</p><p>Regards,<br>Zvertex3D Team<br>${COMMUNICATION_EMAIL}</p></div>`});}
async function sendAdminNotification(subject,html,text){const to=process.env.ADMIN_EMAIL||COMMUNICATION_EMAIL;return sendMail({to,subject,html,text});}
async function sendVendorStatusEmail(vendor,status){const subject=`Zvertex3D vendor application: ${status}`;return sendMail({to:vendor.email,subject,text:`Your Zvertex3D vendor application is now ${status}.`,html:`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Vendor Network</h2><p>Hello ${vendor.name},</p><p>Your vendor application for <strong>${vendor.storeName}</strong> is now <strong>${status}</strong>.</p>${status==='approved'?'<p>Your store is now live in the Zvertex3D Marketplace.</p>':''}<p>Thank you,<br/>Zvertex3D Team<br/>${COMMUNICATION_EMAIL}</p></div>`});}
async function sendOrderEmails(order,vendor){const subject=`Zvertex3D order ${order.orderNumber} received`;const customerHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D</h2><p>Hi ${order.customer.name},</p><p>Your manufacturing request <strong>${order.orderNumber}</strong> has been sent to <strong>${vendor.storeName}</strong>.</p><p>Estimated value: ₹${Number(order.estimatedPrice||0).toLocaleString("en-IN")}</p><p>Zvertex3D will keep you updated.</p><p>${COMMUNICATION_EMAIL}</p></div>`;const vendorHtml=`<div style="font-family:Arial,sans-serif"><h2>Zvertex3D Manufacturing Network</h2><p>New order <strong>${order.orderNumber}</strong> has been assigned to your approved store <strong>${vendor.storeName}</strong>.</p><p>Customer: ${order.customer.name} · ${order.customer.email} · ${order.customer.phone}</p></div>`;await Promise.all([sendMail({to:order.customer.email,subject,text:`Order ${order.orderNumber} received by Zvertex3D.`,html:customerHtml}),sendMail({to:vendor.email,subject,text:`New Zvertex3D order ${order.orderNumber}.`,html:vendorHtml}),sendAdminNotification(subject,vendorHtml,`New order ${order.orderNumber} for ${vendor.storeName}.`)]);}
module.exports={sendMail,sendSignupConfirmation,sendVendorApplicationConfirmation,sendAdminNotification,sendVendorStatusEmail,sendOrderEmails};
>>>>>>> 198dc3267cce374489a058aa74e737c1402dcabb
>>>>>>> 4ff7eff4da625b0aa81943bb92486aeaeca350f5
>>>>>>> bfa6eea49934b7beb4ebc42a1ae246db25ad53c1
>>>>>>> a6a3db8e95390c1e7f8a89f32fc719b550f6b66b
>>>>>>> 6749823fb1f85adf6e3b7e3523847c8ec5eea842
>>>>>>> 5fe4c532631c49952bf7e16bbac69a863f154676
>>>>>>> f5f7d408740acf640b490ad955179b89f222a19c
>>>>>>> 0e9e6f07bd68a8be2c5da3b5054c33dec9b12c54
